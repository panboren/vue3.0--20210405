{
  "name": "vue3.0-note",
  "version": "1.0.0",
  "description": "注册“全局”组件\r const app = Vue.createApp({})\r app.component('SearchInput', SearchInputComponent)\r app.directive('focus', FocusDirective)\r app.use(LocalePlugin)",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
